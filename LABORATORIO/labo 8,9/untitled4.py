def verificar_lista_no_vacia(lista):
    if len(lista) > 0:
        print("La lista no está vacía.")
    else:
        print("La lista está vacía.")

# Definir una lista de ejemplo
lista_ejemplo = [1, 2, 3]

# Verificar si la lista no está vacía
verificar_lista_no_vacia(lista_ejemplo)
